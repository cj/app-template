<div class="d-flex flex-column justify-content-center align-items-center">
  <h2><%= t('.sign_up') %></h2>

  <%= render AuthComponent::SignUp.new({
    resource: resource,
    as: resource_name,
    url: registration_path(resource_name),
  }) %>
</div>
