<%= turbo_frame_tag turbo_id do %>
  <%= flash_messages %>

  <%= form_with(base_options) do %>
    <% if parent_turbo_id %>
      <%= hidden_field(nil, :parent_turbo_component, value: parent_turbo_id, id: parent_turbo_id) %>
    <% end %>

    <%= content %>

    <%= invisible_captcha %>
  <% end %>
<% end %>
